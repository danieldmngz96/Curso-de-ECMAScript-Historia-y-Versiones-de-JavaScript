let numbers = [1,3,4,6,7,8];
console.log(numbers.includes(0)); //Detectamos con un boolean si el numero esta en el array numbers

const list = ['Freddy','Bob','Esponja'];
console.log(list.includes('Bob')); //Detectamos con un boleaan si el string es o no!


